1. 인프라 준비
 - Docker Desktop 설치 완료, 트레이 아이콘 정상 실행되는가?
 - 터미널에서 docker run hello-world 명령어가 에러 없이 실행되는가?

2. 설계도 작성
 - 프로젝트 루트 경로에 확장자 없는 Dockerfile을 생성했는가?
 - FROM XXXXX 에 적절한 베이스 이미지를 설정했는가?
 - requirements.txt를 통해 llama-index 등 필수 라이브러리 설치(RUn pip install)를 명시했는가?

3. 환경 및 통신 연결 
 - docker run 실행 시 -p 옵션을 사용해 컨테이너 내부 포트와 외부(호스트 PC) 포트를 올바르게 연결했는가?

4. 서비스 구동
 - 컨테이너 실행 시 외부 문서(PDF/Text 등)를 docker 내붸어 llama_index로 인덱싱 할 수 있는가?